import { User } from "src/users/interfaces/user.interface";
import { PokerRoom } from "../interfaces/pokerRoom.interface";
export declare function checkBet(room: PokerRoom, raise: boolean, bet: number | number, user: User): any;
