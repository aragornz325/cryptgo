import { Document } from 'mongoose';
import * as mongoose from 'mongoose';
import { GameType, RoomStatus } from "../interfaces/pokerRoom.interface";
export declare type PokerRoomDocument = PokerRoom & Document;
export declare class PokerRoom {
    players: any[];
    maxPlayers: number;
    status: RoomStatus;
    type: GameType;
    password?: string;
    initialBet: number;
    turnBet: number;
    lastRaise: number;
    raises: number;
    currentBet: number;
    pot: number;
    round: number;
    limit: boolean;
    deck?: String[];
    winners: any[];
    deckHand?: any[];
    dealerIndex: number;
    rakeValue?: number;
    entryPrice?: number;
    smallBlind: number;
    bigBlind: number;
    name: string;
    timeout: number;
    timeoutId: number;
}
export declare const PokerRoomSchema: mongoose.Schema<Document<PokerRoom, any, any>, mongoose.Model<Document<PokerRoom, any, any>, any, any, any>, {}>;
